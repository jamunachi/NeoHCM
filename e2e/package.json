{
  "name": "neohcm-e2e",
  "private": true,
  "devDependencies": {
    "@playwright/test": "^1.47.0"
  },
  "scripts": {
    "test": "playwright test",
    "install:browsers": "npx playwright install --with-deps"
  }
}